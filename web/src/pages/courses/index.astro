---
import DefaultLayout from "@/layouts/DefaultLayout.astro";

const word = Astro.url.searchParams.get("word");
---

<DefaultLayout>
	<h1>{word ? `Searching for: ${word}` : "Hello SearchPage"}</h1>
</DefaultLayout>